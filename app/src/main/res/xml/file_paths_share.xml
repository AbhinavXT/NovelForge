<?xml version="1.0" encoding="utf-8"?>
<paths>
    <!-- Exposes cacheDir for share-card PNGs generated by StatsShareExporter.
         Only files we explicitly grant FLAG_GRANT_READ_URI_PERMISSION for
         are actually accessible to other apps. -->
    <cache-path name="shared_images" path="." />
</paths>